window.addEventListener('DOMContentLoaded', () => {

    //Testimonials slider

    const slidesWrapper = document.querySelector('.testimonials__grid');
    const slides = document.querySelectorAll('.grid__user');
    const arrowLeft = document.querySelector('.arrows__btn_left');
    const arrowRight = document.querySelector('.arrows__btn_right');
    const dots = document.querySelectorAll('.btn__dot');
    const widthSliderWrapper = window.getComputedStyle(slidesWrapper).width;
    let slideIndex = 1;
    let offset = window.getComputedStyle(slidesWrapper).width;

});